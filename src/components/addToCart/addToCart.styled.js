import styled from 'styled-components';

const StyledContainer = styled.div`
  display: flex;
`;

const StyledQuantityInput = styled.input`
  display: block;
  margin: 0 10px;
  text-align: center;
  width: 60px;
`;

export { StyledContainer, StyledQuantityInput };
